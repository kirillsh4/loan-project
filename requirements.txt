# Pinned to versions used during training. To match exactly,
# regenerate with `pip freeze > requirements.txt` from your own env —
# sklearn version mismatches will break loading model.joblib.
scikit-learn==1.8.0
pandas==3.0.2
numpy==2.4.4
scipy==1.17.1
joblib==1.5.3
